spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MarketMosaic
    username: root
    password: Sunny@11
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: local # or prod, test, etc., depending on your environment

server:
  port: 8082

logging:
  level:
    root: INFO
    com.example.marketmosaic: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

# Custom application properties
app:
  name: Product Service
  version: 1.0.0